<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <!-- 
  	namespace属性是名称空间的意思。
  	功能相当于 给配置文件定义一个包名。
  	一般情况下。可以写两种值，一种是对应类的全类名
  	一种情况是。对应类的处理接口全类名（后面讲）。
   -->
<mapper namespace="com.atguigu.mybaits.bean.User">
	<!-- 
		select 标签用于定义一个select查询语句
		
		id属性，又称之为statementId
		id属性可以给select语句定义一个唯一的标识
		
		parameterType 属性定义参数的类型，int 表示基本的Integer类型,可以省略，但是如果为bean 类型的话，就好不要省略
		resultType 属性定义返回值的数据类型
	 -->

	<insert id="insertUser" parameterType="com.atguigu.mybaits.bean.User">

		insert into t_user (last_name,sex) values (#{lastName},#{sex})
	</insert>

	<delete id="deleteUserById">
		delete from t_user where id = #{id}
	</delete>

	<update id="updateUser">
		update t_user set last_name = #{lastName} , sex = #{sex} where id = #{id}
	</update>

	<select id="findUserById" resultType="com.atguigu.mybaits.bean.User">
		select id ,last_name lastName , sex from t_user where id = #{id}
	</select>

	<select id="findUsers" resultType="com.atguigu.mybaits.bean.User">
		select id ,last_name lastName , sex from t_user
	</select>


	<!--
	useGeneratedKeys 表示返回生成的主键
	keyProperty 表示把返回的key注入到返回值的哪个属性中
	keyProperty="id" 表示把返回的id主键值注入到返回对象的id属性中
	-->
	<insert id="insertUser1" parameterType="com.atguigu.mybaits.bean.User" useGeneratedKeys="true" keyProperty="id">
		insert into t_user (last_name,sex) values (#{lastName},#{sex})
	</insert>

	<!--
	useGeneratedKeys

	-->
	<insert id="insertUser2" parameterType="com.atguigu.mybaits.bean.User" useGeneratedKeys="true" >
		<selectKey keyProperty="id" resultType="int" order="AFTER">
			SELECT LAST_INSERT_ID()
		</selectKey>
		insert into t_user (last_name,sex) values (#{lastName},#{sex})
	</insert>



	

</mapper>


